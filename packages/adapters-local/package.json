{
  "name": "@au-archive/adapters-local",
  "version": "0.1.0",
  "description": "Local adapter implementations for au-archive (SQLite, LocalStorage, ExifTool)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "test:run": "vitest --run",
    "lint": "eslint src/"
  },
  "dependencies": {
    "@au-archive/import-core": "workspace:*",
    "better-sqlite3": "^11.5.0",
    "exiftool-vendored": "^28.3.1",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.11",
    "@types/uuid": "^10.0.0",
    "vitest": "^1.6.0",
    "typescript": "^5.4.0"
  },
  "peerDependencies": {
    "@au-archive/import-core": "workspace:*"
  }
}
