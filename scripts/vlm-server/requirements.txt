# VLM Server Requirements
# Qwen2-VL 7B for deep image analysis
# Setup: python3.12 -m venv venv && source venv/bin/activate && pip install -r requirements.txt

torch>=2.0.0
torchvision>=0.15.0
transformers>=4.45.0
accelerate>=0.25.0
pillow>=10.0.0
qwen-vl-utils  # Qwen VL utilities for image processing
